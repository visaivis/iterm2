# Agent environment variables — resolved at runtime by `op run`
# This file contains op:// references (not secrets) and is safe to commit.
# Usage: op run --env-file=.env.agent -- opencode
GH_TOKEN=op://iterm2-agents/iterm2-agent-pat/password
